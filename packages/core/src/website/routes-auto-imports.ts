// Auto Generated by '@starnexus/import-routes'
import { router as githubcom } from './github.com/router'
import { pathInfo as githubcomRepo } from './github.com/paths/repo'
import { router as twittercom } from './twitter.com/router'
import { pathInfo as twittercomTweet } from './twitter.com/paths/tweet'

githubcom.paths = [
  githubcomRepo,
].sort((a, b) => (b.sequence ?? 0) - (a.sequence ?? 0))
twittercom.paths = [
  twittercomTweet,
].sort((a, b) => (b.sequence ?? 0) - (a.sequence ?? 0))

export const routes = {
  'github.com': githubcom,
  'twitter.com': twittercom,
}
